package algorithms.search;

import algorithms.mazeGenerators.Position;

import java.util.ArrayList;

/** The solution class will show an arraylist that represents the path between the start and goal positions, included */
public class Solution {
    /** An arraylist that represents the path. It will start with the start Position in the first place, and the goal
     * in the last.
     */
    ArrayList<Position> path;
}
